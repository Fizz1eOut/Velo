<template>
  <h2 class="title">
    <slot></slot>
  </h2>
</template>
<style scoped>
  .title {
    font-family: var(--ff-display);
    font-size: var(--fs-lg);
    font-weight: var(--fw-semibold);
    color: var(--text-primary);  
  } 
  @media (max-width: 768px) {
    .title {
      font-size: var(--fs-md);
    }
  }
</style>